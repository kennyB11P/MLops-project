# DB adapters

Здесь можно хранить отдельные адаптеры подключения:

- `postgres.py` — пул соединений PostgreSQL;
- `qdrant.py` — клиент Qdrant;
- миграции — позже можно перенести в Alembic.

Сейчас подключения находятся прямо в tools, чтобы MVP был проще.
